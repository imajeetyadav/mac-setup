brew update           # download updated formulas
brew outdated         # what's old?
brew upgrade          # upgrade all formulae
# list all brew casks installed & their versions
brew outdated --cask
brew upgrade --cask
# cleanup everything
brew cleanup
